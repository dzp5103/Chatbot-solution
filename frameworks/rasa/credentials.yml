# Configuration for connecting to external services and APIs

rest:
  # webhook_url: "http://localhost:5005/webhooks/rest/webhook"

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Slack configuration (uncomment to enable)
# slack:
#   slack_token: "xoxb-your-slack-token"
#   slack_channel: "your-channel"
#   slack_signing_secret: "your-signing-secret"

# Facebook Messenger configuration (uncomment to enable)
# facebook:
#   verify: "your-verify-token"
#   secret: "your-app-secret"
#   page-access-token: "your-page-access-token"

# Telegram configuration (uncomment to enable)
# telegram:
#   access_token: "your-telegram-token"
#   verify: "your-verify-token"
#   webhook_url: "https://your-domain/webhooks/telegram/webhook"